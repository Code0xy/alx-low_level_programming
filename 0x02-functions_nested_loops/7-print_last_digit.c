#include "main.h"

/**
 * print_last_digit -> prints the last digit
 * @n: the passed arg
 * Return: the last digit
 */
int print_last_digit(int n)
{
int x;

if (n < 0)
n = -n;
_putchar((n% 10) + '0')
_putchar(x + '0');

return (x);
}
