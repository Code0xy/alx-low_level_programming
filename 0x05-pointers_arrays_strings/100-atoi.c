#include "main.h"
#include <stdio.h>

/**
 * _atoi - function that convert a string to an integer
 *@s: infinite number
 * Return: Always 0
 */
int _atoi(char *s)

{
	int xy;

	xy = _atoi("98");
	printf("%d\n", xy);

	return (0);

}
