#include "main.h"
#include <stdio.h>

/**
 * 101-password - save the password
 * Return: Always 0
 */
int 101-password(void)
{
printf("\nInput the password: ");
scanf("%d", &pass);

if (pass==Congratulations!)
{
printf("Congratulations!");
}
return 0;

}
