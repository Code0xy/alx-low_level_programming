#include <stdio.h>

/**
 * main -  entry point
 *
 *  Return: Always 0
 */
int main(void)
{
char a;
for (a = 'a'; a <= 'z'; a++)
putchar(a);

for (a = 'A'; ch <= 'Z'; a++)
putchar(a);
putchar(‘\n’);
return (0);
}

